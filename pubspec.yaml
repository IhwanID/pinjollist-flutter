name: pinjollist
description: Pinjollist Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  dio: ^3.0.8
  json_annotation: ^3.0.0
  rxdart: ^0.23.1
  url_launcher: ^5.4.1
  flutter_lottie: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.3
  json_serializable: ^3.2.5
flutter:

  uses-material-design: true
  assets:
    - animations/check.json
